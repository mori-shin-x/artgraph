pre-commit:
  parallel: true
  commands:
    oxfmt:
      glob: "*.{ts,tsx,mts,cts,js,mjs,cjs}"
      run: pnpm exec oxfmt --write {staged_files}
      stage_fixed: true
    oxlint:
      glob: "*.{ts,tsx,mts,cts,js,mjs,cjs}"
      run: pnpm exec oxlint {staged_files}

pre-push:
  parallel: false
  commands:
    typecheck:
      run: bash scripts/hook-run.sh pnpm typecheck
    knip:
      run: bash scripts/hook-run.sh pnpm knip
    test-unit:
      run: bash scripts/hook-run.sh pnpm test:unit
    test-e2e:
      run: bash scripts/hook-run.sh pnpm test:e2e
