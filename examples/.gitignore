# Artifacts produced by running the examples — keep them out of git so the
# starting state stays reproducible.
**/.trace.lock
speckit-integration/.specify/extensions/spectrace/
kiro-integration/.kiro/steering/
