# Tasks (mixed)

`tasks` is a shared stem in both kiro and spec-kit, so in this mixed dir it
ends up with *two* parent chains (one to `design`, one to `plan`). This file
locks in that intentional behavior.
